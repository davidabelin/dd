"""Model runtime package for Double-digits.

The package owns the notebook-derived Keras backend bootstrap, preset registry,
artifact-backed classifiers, and the shared runtime used by both the web and
CLI surfaces.
"""
